mcs.metadata:
  componentName: GCF REST Connector - Batch generate custom connectors
kind: TaskDialog
modelDisplayName: Batch generate custom connectors
modelDescription: Generate Power Platform custom connector Swagger files for multiple connector groups in a single call.
inputs:
  - kind: AutomaticTaskInput
    propertyName: groupsJson
    name: Groups JSON
    description: JSON string containing an array of connector groups
  - kind: AutomaticTaskInput
    propertyName: version
    name: API Version
    description: Graph API version (v1.0 or beta)

outputs:
  - propertyName: groupCount

  - propertyName: resultsJson

  - propertyName: successCount

action:
  kind: InvokeConnectorTaskAction
  connectionReference: copilots_header_91a5e.shared_new-5fgcf-20rest-20connector-5f251f982b189de9d2.3b64b1b7818145a59f83da81404dde8e
  connectionProperties:
    mode: Invoker

  operationId: graph_batchGenerate

  inputType:
    properties:
      groupsJson:
        displayName: Groups JSON
        description: JSON string containing an array of connector groups
        isRequired: true
        type: String
      version:
        displayName: API Version
        description: Graph API version (v1.0 or beta)
        isRequired: false
        type: String

outputMode: All
